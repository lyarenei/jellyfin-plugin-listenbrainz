name: 'Test Plugin'

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**/*.md'
      - '**/*.yaml'
  pull_request:
    branches:
      - main
    paths-ignore:
      - '**/*.md'
      - '**/*.yaml'
  workflow_dispatch:

jobs:
  call:
    uses: jellyfin/jellyfin-meta-plugins/.github/workflows/test.yaml@master
